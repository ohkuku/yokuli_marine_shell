<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:name=".ShellApplication" android:allowBackup="false" android:label="Yokuli OS" android:supportsRtl="true" android:theme="@style/Theme.YokuliOS">
        <activity android:name=".ShellActivity" android:exported="true" android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
