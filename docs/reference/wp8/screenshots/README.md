# WP8 原始截图合同

状态：`NOT_YET_MEASURED`。

本目录未来只保存合法取得、可追溯的 WP8 原始参考截图。每个文件必须在 `WP8_REFERENCE_MEASUREMENTS.json` 的 `provenance.captureFiles` 中出现，并记录设备或模拟器、OS build、viewport、采集方式、UTC 时间、所有权/许可说明。禁止裁剪后冒充原图，禁止把 Yokuli 实现截图放入本目录。

命名格式：

```text
wp8_classic_phone_4col__<surface>__<theme>__<width>x<height>__source.<ext>
```

Stage 0 不提交任何参考媒体；实际采集和权利检查等待人工 Reference Lab。

## English translation

Status is `NOT_YET_MEASURED`. This directory is reserved for legally obtained, traceable WP8 source captures. Every file must be referenced by the measurement provenance with device or emulator, OS build, viewport, capture method, UTC timestamp, and ownership or license note. Yokuli implementation screenshots must never be stored here as reference evidence. Stage 0 adds no media.
