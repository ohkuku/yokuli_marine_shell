# CL01 · SAF原件与FD SQLite读取

前置：CL00。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 5、9、16 节。
- `../IMPLEMENTATION_MAP.md` 的 R04, R07, R12, R14 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

实现可复用最小read-only FD reader及必要窄core接口。以真实DocumentProvider取r权限，验证seekable、64-bit offset、metadata/样本tile查询、fd所有权、取消和静态DB/锁限制。优先按合同使用FD VFS；不复制原件、不写SQLite解析器、不依赖猜真实路径。

## 门禁

local SAF原件实际读出正确tile；pipe/子区间未支持时明确拒绝；损坏/短读/大offset有测试；原件hash不变且应用无整库副本。只完成读取，不标地图显示已完成。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
