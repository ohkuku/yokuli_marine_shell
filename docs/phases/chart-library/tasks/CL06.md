# CL06 · Runtime、会话与外部变更

前置：CL05。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 5.3、7–8、12–13、16 节。
- `../IMPLEMENTATION_MAP.md` 的 R03, R10 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

完成process-owned Library runtime、catalog发布、source generation与read leases。取消/撤权/来源离线/替换/重新关联时有序失效会话与revision缓存；限制fd/worker/任务；进程重启恢复索引和中断状态。Chart不再依赖管理UI触发资源刷新。

## 门禁

无UI消费者也可管理任务；关闭Library不关闭Chart lease；旧回调/旧tile结果不污染新revision；进程重启不复活fd；不借用NMEA FGS。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
