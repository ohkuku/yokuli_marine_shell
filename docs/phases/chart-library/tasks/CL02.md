# CL02 · MapLibre零复制显示闭环

前置：CL01。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 5.2–6、10.2、16 节。
- `../IMPLEMENTATION_MAP.md` 的 R05, R06, R14 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

在现有MapLibre13.4.1接内部loopback TileSet桥，通过CL01读取真实SAF tile。限定loopback/token/asset/revision/GET，限制缓存和并发；正确处理启动、取消、缺片和错误。保留MapView生命周期与现有overlay，不另建地图引擎。

## 门禁

instrumentation实际出图/移动/关开地图/无外网测试；原件不变、无整库复制、无LAN监听、token失效生效。CL01+02联合门禁通过后才进入完整产品集成；不能用Fake reader冒充。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
