# CL00 · 基线确认与合同覆盖

前置：无。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 0–2、13–15、18 节。
- `../IMPLEMENTATION_MAP.md` 的 R01, R02, R09, R14 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

只核对实际branch/HEAD/dirty state与实施地图差异；定位安装门禁、旧import路由、测试任务和native构建条件。形成短基线/符号映射，标记当前NMEA并行改动；拟定当前产品面覆盖，不改历史报告。此任务不写产品实现。

## 门禁

基线和要更新的有效合同/断言路径清楚；没有checkout/reset/rebase、旧仓写入或产品代码改动；未运行的基线测试明确列出。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
