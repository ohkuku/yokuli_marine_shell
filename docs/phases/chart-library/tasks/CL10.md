# CL10 · Shell安装、磁贴与跨应用导航

前置：CL07, CL08, CL09。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 0、2、15 节。
- `../IMPLEMENTATION_MAP.md` 的 R01, R02, R09, R10, R11 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

添加唯一Library InstalledAppBinding、All Apps/搜索/三尺寸磁贴/neutral状态摘要；将Chart和Settings维护入口深链到Library，处理旧路由恢复。更新有效产品合同及明确的installed-app断言，保留当前其它应用和已有Start布局。

## 门禁

A01/A22/A38–A40；三尺寸双语主题真实状态，Back/Start/caller恢复正确，未自动固定；无debug占位；不以新白名单删掉当前NMEA应用。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
