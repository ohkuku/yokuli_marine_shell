# CL12 · 最终回归与产品验收

前置：CL11。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 17–19 节。
- `../IMPLEMENTATION_MAP.md` 的 R01, R02 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

运行当前完整质量/安装/依赖/翻译/迁移/release门禁和本轮instrumentation。逐项填写A01–A42实际状态/命令/证据，记录支持的provider/编码/限制。修复本轮回归并复跑，提供短最终交付说明。

## 门禁

真实文件夹→独立海图库→Chart零复制闭环可复现；没有未运行项被计PASS；source/user数据未破坏；未push/merge/release；阶段状态与证据一致。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
