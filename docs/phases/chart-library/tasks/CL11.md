# CL11 · 故障、数据安全与资源压力

前置：CL10。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 5–6、8–12、14、16–17 节。
- `../IMPLEMENTATION_MAP.md` 的 R04, R05, R06, R10 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

按A02–A42补跨边界故障测试：provider pipe/撤权/部分枚举、外部替换、并发读删、迁移崩溃、native ABI、loopback攻击面、1000项/30min有界负载。采集源读字节/副本字节/fd/内存/任务等证据，不仅截图。

## 门禁

报告synthetic和实机证据差异；零复制检查独立通过；所有缺失设备/性能项标NOT_RUN；没有为绿测试放开任意文件路径或关闭安全门禁。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
