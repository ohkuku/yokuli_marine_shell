# CL04 · 来源授权与增量目录扫描

前置：CL03。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 3–4、8、12 节。
- `../IMPLEMENTATION_MAP.md` 的 R10 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

实现folder和single-document picker effect接收、持久read grant和引用计数、递归枚举、差异检测与有界验证队列。支持真实进度/取消/手动刷新/部分扫描；pending文件和坏文件可解释；MISSING只能基于成功完整枚举判定。

## 门禁

树/单文件/多层/重叠授权/撤权/取消/失败子树/provider未知size/重复扫描有测试；删除一个来源不影响其它授权；不打开Chart或请求NMEA/定位权限。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
