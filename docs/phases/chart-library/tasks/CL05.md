# CL05 · 分层验证与共享瓦片查询

前置：CL04。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 5.3、9、16 节。
- `../IMPLEMENTATION_MAP.md` 的 R04, R06, R07, R12 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

通过统一reader实现basic inspection、可取消full verification与revision绑定结果。支持PNG/JPEG/WebP、256/512、tiles view、TMS/XYZ query-time转换；保留必要输入安全、SQL/解码预算。自动扫描不做全库copy/hash/decode；无法便宜取得的统计保持unknown。

## 门禁

A14–A21目标测试；renderer和coverage得到同一坐标映射；外部原件零写入；BASIC不冒充FULL；文件验证中改变不发布通过。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
