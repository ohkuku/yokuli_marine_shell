# CL07 · 独立海图库应用页面

前置：CL06。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 2–4、7、11–12、15 节。
- `../IMPLEMENTATION_MAP.md` 的 R03, R11 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

实现独立feature的UiState/UiAction/projector与来源首页、来源详情、资源详情、搜索/筛选/批量启停、权限修复、验证/复制进度及存储状态。只接core端口。UI真值来自catalog/job事务；维持WP8视觉和双语。

## 门禁

单独host中无Chart也能添加、刷新、修复和管理；未知/错误/空态真实；不创建每项MapView；移除资源不会删外部原件；UI相关目标测试通过。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
