# CL08 · Chart显示消费、多图与覆盖

前置：CL06。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 2–3、6、10、13 节。
- `../IMPLEMENTATION_MAP.md` 的 R03, R05, R06, R07 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

移出Chart资源维护，接只读catalog和display plan。支持固定单图/选定来源集合、角色/优先级/透明度、多区域候选、相同reader的真实tile覆盖检查。更新单activePackage相关reducer与renderer接线；保留overlay、相机、路线操作。

## 门禁

A23–A30；新增图不抢固定选择，bounds空洞不假通过，跨日期线/不同size正确，旧revision结果失效；Chart无SAF/扫描/CRUD；不回退在线Google填洞。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
