# CL09 · 旧受管包迁移与显式副本

前置：CL06。执行共同约束见 `../START_HERE.md`；不在每次对话重复全文。

## 本轮只读

- `../REQUIREMENTS.md` 第 11、14 节。
- `../IMPLEMENTATION_MAP.md` 的 R04, R07, R09, R10 及此任务直接依赖。
- `../EXECUTION_STATE.json` 中已完成工单的真实输出路径/当前HEAD。

## 实施范围

抽取/接入legacy managed backend，幂等登记已有map_packages并保留ID/history/journal/active choice。接显式单张复制、失败恢复、空间限制、lease保护删除与清缓存；关联外部原件和副本。不得重新复制所有老包。

## 门禁

A31–A35；使用实际旧格式fixtures覆盖中断恢复；用户地图/桌面数据不变；不读旧Anchor app私有目录；副本完成不自动抢图；copy/scan/renderer并发安全。

## 不做

不实现本工单外页面/架构，不重新扫旧仓；不因短日志而少跑影响范围内的测试。路径不存在/合同冲突时才扩读，记录实际映射。

## 输出

更新 EXECUTION_STATE.json 对应项：status、head、changed_files、commands/exit_code、evidence、deviations/blockers。成功日志留文件；最终回复只列变动、测试与阻塞，不重抄完整需求。
